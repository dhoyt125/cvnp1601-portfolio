# Evidence 

":q!" is a command inside of vim that exit the file without saving since the last save.


Double grep -v pipeline has two stages, one remvoes lines matching one pattern like blan lines, and the second 
removes lines matching another pattern like comments starting with "#". Chaining them together 
strips out both noises to clean up the output. 


"-F:" tells "awk" to use colon as the separator insdead of a blank space between the fields. Without the
use of "-F:" awk would treat the entire line one single field. 


 



